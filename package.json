{
  "name": "assent-kernel",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic policy kernel for Assent — the Change primitive + pure policy() trust decision.",
  "type": "module",
  "engines": {
    "node": ">=22.6.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "@types/node": "^26.2.0"
  }
}
